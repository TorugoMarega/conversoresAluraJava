package model;

import java.math.BigDecimal;

public class Time extends Measure{
    public Time(Integer id,String name, String symbol, BigDecimal value) {
        super(id,name, symbol, value);
    }
}
